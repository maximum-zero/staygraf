# STAYGRAF

STAYGRAF는 완성된 공간 콘텐츠인 GRAF와 전문 인테리어 자재 탐색·구매 경험을 연결하는 서비스다.

## Requirements

- Node.js 24 이상
- pnpm 11 이상

## Commands

```bash
pnpm install
pnpm dev
pnpm lint
pnpm format:check
pnpm typecheck
pnpm test
pnpm test:e2e
pnpm build
```

제품 범위와 구현 규칙은 `docs/`와 `AGENTS.md`를 참고한다.
