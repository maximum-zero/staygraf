# STAYGRAF 작업 규칙

## 1. 기준 문서와 우선순위

작업 전에 `docs/.pdca-status.json`과 현재 기능의 Plan·Design 문서를 확인한다.

충돌이 있으면 다음 순서를 따른다.

1. 사용자가 대화에서 명시적으로 승인한 최신 결정
2. 현재 기능의 승인된 Plan 문서
3. 현재 기능의 승인된 Design 문서
4. 이 문서의 작업 규칙
5. 그 밖의 참고 문서

결정되지 않은 내용은 요구사항으로 간주하지 않는다. 제품 정책을 이 문서에 복제하지 않고 기준 문서를 링크해 참조한다.

- Plan: `docs/01-plan/features/staygraf-mvp.plan.md`
- Design: `docs/02-design/features/staygraf-mvp.design.md`

## 2. 승인 기반 작업 절차

- 기획, UX, UI, 아키텍처와 기술 선택은 대안과 장단점을 먼저 제시한다.
- 사용자의 명시적인 승인 전에 애플리케이션 코드, 설정 또는 의존성을 추가하지 않는다.
- 승인된 범위를 넘어 기능, 정책, 예외 또는 추상화를 임의로 추가하지 않는다.
- 새 결정이 기존 문서와 충돌하면 코드를 작성하기 전에 차이를 알리고 재승인받는다.
- 사용자 화면을 먼저 구현·검증하고, 승인된 화면을 토대로 ERD·API·백엔드를 설계한다.

## 3. 역할별 검토

복잡한 기능과 화면은 다음 관점을 구분해 검토한다.

- 기획자: 목적, 사용자 가치, 필수 정보와 정책 누락
- UX 디자이너: 여정, 우선순위, 상태 변화와 오류 회복
- UI 디자이너: 시각적 계층, 일관성, 반응형과 접근성
- 개발자: 책임 경계, 구현 난이도, 성능과 유지보수성
- QA·리뷰어: 성공 기준, 예외 상태와 회귀 위험

의견을 하나로 가장하지 않는다. 관점별 차이를 제시하고 사용자가 최종 선택하도록 한다.

## 4. 프로젝트 경계

- 패키지 매니저는 `pnpm`을 사용한다.
- 저장소는 가벼운 pnpm workspace로 시작한다.
- 초기 사용자 앱은 `apps/web`에 둔다.
- 관리자 구현이 승인되면 `apps/admin`을 별도 앱으로 추가한다.
- `packages/*`는 실제 공유 코드가 확인된 뒤 승인받아 추가한다.
- 일반 회원과 관리자 인증·데이터 모델을 하나로 합치지 않는다.
- 백엔드, 데이터베이스와 인프라 스택은 사용자 UI 승인 후 결정한다.

## 5. 승인된 프론트엔드 기준

- Next.js App Router와 TypeScript strict를 사용한다.
- Tailwind CSS를 중심으로 스타일링하고 의미 기반 디자인 토큰을 사용한다.
- 전역 CSS는 토큰, 초기화와 전역 기반 스타일로 제한한다.
- CSS Modules는 필요한 예외가 확인되고 승인된 경우에만 추가한다.
- shadcn/ui는 접근성 기반 범용 요소만 선택적으로 가져와 프로젝트 소스로 관리한다.
- shadcn/ui 블록·페이지 템플릿과 기본 외형을 완성 디자인으로 사용하지 않는다.
- GRAF, 상품 카드·갤러리·옵션, 필터, 계산기, 배송 그룹 장바구니와 견적서는 전용 컴포넌트로 설계한다.
- 아이콘은 승인된 단일 아이콘 체계를 사용하며 이모지를 UI 아이콘으로 대체하지 않는다.

## 6. 상태와 폼

- 검색, 필터와 정렬 상태는 URL Search Params에 둔다.
- 옵션, 갤러리와 계산기처럼 한 화면에 머무는 상태는 React 로컬 상태를 우선한다.
- Zustand와 persist는 비회원 장바구니에만 사용한다.
- 로그인, 가격 문의, 견적과 주문 폼은 React Hook Form과 Zod를 사용한다.
- 백엔드 결정 전 서버 상태 라이브러리를 도입하지 않는다.

## 7. 코딩 컨벤션

### TypeScript와 React

- `strict`를 활성화한다.
- `any`는 원칙적으로 사용하지 않으며 입력 경계에서는 검증 후 타입을 좁힌다.
- 타입 단언보다 스키마 검증, 판별 유니온과 타입 가드를 우선한다.
- 함수 컴포넌트와 Server Component를 기본값으로 사용한다.
- 브라우저 상태, 이벤트 또는 브라우저 API가 필요한 최소 경계에만 `'use client'`를 둔다.
- 재사용 가능성의 추측보다 실제 반복과 명확한 책임을 기준으로 컴포넌트를 분리한다.

### 명명과 파일

- 컴포넌트와 컴포넌트 파일: `PascalCase`
- 함수, 변수와 훅: `camelCase`
- 상수: `UPPER_SNAKE_CASE`
- 폴더와 일반 파일: `kebab-case`
- boolean: `is`, `has`, `can`, `should` 접두사
- 경로 별칭은 우선 `@/*` 하나만 사용한다.
- 깊은 barrel export와 순환 참조를 만들지 않는다.

### 배치와 공용화

- 라우트 전용 코드는 해당 라우트 가까이에 둔다.
- 여러 화면에서 실제로 공유될 때만 공용 영역으로 이동한다.
- 도메인 컴포넌트와 범용 UI 컴포넌트를 구분한다.
- 샘플 데이터는 UI 컴포넌트와 분리한다.
- 테스트 파일은 대상 코드 가까이에 두고 Playwright 테스트만 `e2e/`에서 관리한다.

## 8. UI·이미지 품질

- UI 설계 전 UI UX Pro Max를 사용해 적합한 패턴과 대안을 검토한다.
- 실제 제품을 참고하되 상표, 로고, 이미지와 고유 자산을 복제하지 않는다.
- 상품 이미지는 중립적인 프리미엄 카탈로그 품질, GRAF 이미지는 따뜻하고 현실적인 공간 편집 이미지 품질을 지향한다.
- 이미지 생성은 아트 디렉션과 파일럿 결과를 사용자에게 승인받은 뒤 확장한다.
- 로딩, 빈 상태, 오류, 비활성화와 완료 상태를 화면 설계에 포함한다.
- 의미 있는 HTML, 키보드 조작, 포커스 표시, 대체 텍스트와 충분한 대비를 보장한다.
- 모바일과 데스크톱을 모두 검증한다.

## 9. 품질 검증

구현 단계에서는 다음 명령이 통과해야 완료로 판단한다.

- `pnpm lint`
- `pnpm format:check`
- `pnpm typecheck`
- `pnpm test`
- `pnpm test:e2e`
- `pnpm build`

Vitest와 React Testing Library로 계산·상태·컴포넌트 동작을 검증하고 Playwright로 핵심 여정을 검증한다. 핵심 화면은 axe 기반 검사와 수동 키보드 검사를 함께 수행한다.

Storybook은 디자인 토큰과 공유 컴포넌트가 안정된 시점에 도입 여부를 재검토한다. MSW는 API 계약 설계 단계까지 도입하지 않는다.

## 10. Git과 보안

- 코드, 필수 설정, 승인된 제품·설계·결정 문서와 검증 결과만 추적한다.
- `pnpm-lock.yaml`을 커밋한다.
- 커밋 제목은 `<영문 prefix>: <명사형 한글 제목>` 형식을 사용한다.
- 커밋 prefix는 `feat`, `fix`, `docs`, `test`, `refactor`, `chore` 등 변경 성격에 맞게 선택한다.
- 변경 내용이 많으면 빈 줄 뒤에 한글 불릿 목록으로 상세 내용을 기록한다.
- 비밀값, `.env`, 로컬 캐시, 빌드 결과, 테스트 결과와 승인되지 않은 이미지 중간 생성물을 커밋하지 않는다.
- 환경 변수는 이름만 포함한 예제 파일로 관리한다.
- 사용자 변경사항과 무관한 파일을 되돌리거나 덮어쓰지 않는다.
- 승인되지 않은 의존성을 추가하지 않는다.

## 11. PDCA

Plan → Design → Do → Check → Report 순서로 진행한다. 각 단계의 문서와 사용자 승인이 다음 단계의 품질 게이트다. 구현 전 bkit 사전 쓰기 검사를 수행하고, 검증 없이 단계를 완료 처리하지 않는다.
