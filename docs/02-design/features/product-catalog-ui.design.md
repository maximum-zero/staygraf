# STAYGRAF 상품 탐색 UI - Design Document

> Version: 1.0.0 | Date: 2026-08-10 | Status: Approved
> Level: Starter | Plan: [product-catalog-ui.plan.md](../../01-plan/features/product-catalog-ui.plan.md)

---

## 1. Overview

이 문서는 STAYGRAF의 상품 목록·필터와 상품 상세 화면을 브라우저에서 검증하기 위한 UI 설계를 정의한다. 첫 검증 카테고리는 `타일`이며 2depth `타일 / 빅슬랩`을 제공한다. 사용자 승인 전에는 API, ERD와 관리자 속성 편집 구조를 확정하지 않는다.

## 2. Screen Inventory

### 2.1 Product List

- 기본 목록
- 2depth 전환 결과
- 필터가 적용된 목록
- 데스크톱 필터 사이드 패널
- 모바일 필터 바텀시트
- 정렬 메뉴
- 로딩 스켈레톤
- 빈 결과

### 2.2 Product Detail

- 구매 가능 상품
- 가격 문의 상품
- 품절 상품
- 옵션 선택 상태
- 타일 면적 계산기
- 배송 방식 선택
- 관련 GRAF

## 3. Navigation and Routes

```text
Header SHOP
├── /shop/tile?type=tile       타일 > 타일
├── /shop/tile?type=big-slab   타일 > 빅슬랩
├── /shop/faucet               수전
└── /shop/lighting             조명

Product list -> /products/{slug}
Product detail -> cart | buy | price inquiry | related GRAF
```

- 전체 상품 목록 경로는 제공하지 않는다.
- `SHOP`은 카테고리를 드러내는 탐색 트리거이며 사용자는 최소 1depth를 선택해 목록으로 이동한다.
- 필터와 정렬은 목록 URL에 직렬화한다.
- 옵션 기반 필터로 진입한 경우 일치 옵션을 상품 상세의 초기 선택값으로 전달한다.

## 4. Product List Layout

### 4.1 Desktop Wireframe

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ Header                                                                   │
├──────────────────────────────────────────────────────────────────────────┤
│ 카테고리 │ 타일 > 빅슬랩                                                 │
│ 타일     │                                                               │
│ ├ 타일   │ 120개의 상품                          [필터 3] [추천순⌄]       │
│ └ 빅슬랩 │ [포세린 ×] [600×600 ×]                     [초기화]           │
│                                                                          │
│          │ [상품 카드] [상품 카드] [상품 카드] [상품 카드]               │
│          │ [상품 카드] [상품 카드] [상품 카드] [상품 카드]               │
└──────────────────────────────────────────────────────────────────────────┘
                                                       ┌───────────────────┐
                                                       │ 필터              │
                                                       │ 규격              │
                                                       │ 색상              │
                                                       │ 패턴              │
                                                       │ 표면              │
                                                       │ 용도              │
                                                       │ 스타일            │
                                                       │ 가격              │
                                                       │ 브랜드            │
                                                       │ [초기화] [적용]   │
                                                       └───────────────────┘
```

### 4.2 Mobile Wireframe

```text
┌────────────────────────────┐
│ Header                     │
├────────────────────────────┤
│ 타일 > 빅슬랩              │
│ 120개의 상품 [필터 3][추천순]│
│ [포세린×][600×600×] →      │
│                            │
│ [       상품 카드        ] │
│ [       상품 카드        ] │
└────────────────────────────┘

필터 선택 시
┌────────────────────────────┐
│ 필터                    [×]│
│ 규격                       │
│ [300×300] [600×600] ...    │
│ 색상                       │
│ [화이트] [아이보리] ...    │
│ ...                        │
│ [초기화]      [120개 보기] │
└────────────────────────────┘
```

### 4.3 Responsive Grid

| Viewport | Product Grid | Filter UI | Category Navigation |
|---|---:|---|---|
| 320~539px | 1열 | 전체 폭 바텀시트 | 헤더 메뉴 |
| 540~767px | 2열 | 전체 폭 바텀시트 | 헤더 메뉴 |
| 768~1023px | 3열 | 중앙 바텀시트(최대 640px) | 헤더 메뉴 |
| 1024~1239px | 4열 | 우측 420px 패널 | 헤더 메뉴 |
| 1240px 이상 | 좌측 레일 + 4열 | 우측 420px 패널 | 현재 분기 자동 확장 레일 |

### 4.4 Visual Hierarchy

- 목록 상단은 `현재 카테고리 경로 → 결과·필터·정렬 → 적용 조건 → 상품` 순서로 구성한다.
- 큰 시각적 H1은 두지 않고 스크린리더용 H1과 동일 크기의 브레드크럼으로 현재 위치를 제공한다.
- 넓은 데스크톱의 좌측 레일은 현재 1depth만 펼치며 활성 2depth를 브랜드 색상과 왼쪽 선으로 표시한다.
- 결과 개수는 13px 보조 정보, 필터·정렬은 최소 44px 조작 영역으로 제공한다.
- 적용 조건은 36px 칩으로 결과 행 아래에 노출하고 상품 그리드와 24px 간격을 둔다.
- 모바일 상품명과 가격은 14px을 기준으로 하며 상품 카드는 한 열로 제공한다.

## 5. Filter Model

### 5.1 Filter Layers

1. `1depth`: 타일
2. `2depth`: 타일 / 빅슬랩
3. `공통 필터`: 브랜드, 색상, 가격 방식
4. `타일 전용 필터`: 스타일, 용도, 규격, 패턴, 표면
5. `빅슬랩 전용 필터`: 용도, 규격, 두께, 패턴, 표면
6. `옵션 기반 필터`: 색상, 규격, 마감처럼 옵션에 따라 달라지는 속성

### 5.2 Initial Tile Filter Candidates

| Order | Filter | Example Values | Role | Initial Decision |
|---:|---|---|---|---|
| 1 | 규격 | 300×300, 300×600, 600×600, 600×1200, 기타 | 구매·시공 판단 | Must |
| 2 | 용도 | 벽, 바닥, 벽·바닥 | 시공 가능 범위 | Must |
| 3 | 스타일 | 포세린, 폴리싱, 도기질, 모자이크, 기타 | 제품 성격 분류 | Must |
| 4 | 색상 | 화이트, 아이보리, 베이지, 그레이, 블랙, 브라운, 컬러 | 시각 탐색 | Must |
| 5 | 패턴 | 솔리드, 스톤, 마블, 콘크리트, 테라조, 우드 | 일반 사용자 탐색 | Must |
| 6 | 표면 | 무광, 유광, 소프트 매트, 논슬립 | 촉감·용도 판단 | Must |
| 7 | 브랜드 | 데이터에 존재하는 브랜드 | 특정 제품 탐색 | Must |
| 8 | 가격 방식 | 구매 가능, 가격 문의 | 구매 행동 | Must |

### 5.3 Initial Big Slab Filter Candidates

| Order | Filter | Example Values | Role | Initial Decision |
|---:|---|---|---|---|
| 1 | 규격 | 1200×2400, 1200×2600, 1600×3200, 기타 | 제작·운송 판단 | Must |
| 2 | 두께 | 6mm, 9mm, 12mm, 기타 | 시공·가공 판단 | Must |
| 3 | 용도 | 벽, 바닥, 상판, 가구 마감 | 시공 가능 범위 | Must |
| 4 | 색상 | 화이트, 아이보리, 베이지, 그레이, 블랙, 브라운 | 시각 탐색 | Must |
| 5 | 패턴 | 스톤, 마블, 콘크리트, 메탈, 솔리드 | 일반 사용자 탐색 | Must |
| 6 | 표면 | 무광, 유광, 소프트 매트 | 재료 표현 | Must |
| 7 | 브랜드 | 데이터에 존재하는 브랜드 | 특정 제품 탐색 | Must |
| 8 | 가격 방식 | 구매 가능, 가격 문의 | 구매 행동 | Must |

### 5.4 Filter Panel UI

- 데스크톱 측면 패널과 모바일 바텀시트는 동일한 필터 그룹 순서를 사용한다.
- 각 그룹은 다중 확장 가능한 아코디언이며 처음에는 `규격`만 펼친다.
- 선택된 그룹은 우측에 선택 개수를 표시하고 옵션은 GRAF와 같은 pill 선택 상태를 사용한다.
- 반복 구분선은 제거하고 기술 조건, 디자인 조건, 구매 조건 사이에만 두 개의 구분선을 사용한다.
- 하단에는 `초기화`와 `N개 상품 보기`를 고정한다.

### 5.5 Deferred Filters

- 원산지
- 실내·실외
- 공간 추천(거실·주방·욕실 등)
- 미끄럼 저항 등급
- 흡수율과 내마모 등급
- 재고·당일 출고
- 배송 방식
- GRAF 연결 여부

위 항목은 실제 데이터의 신뢰도와 사용자 요구를 확인한 뒤 추가한다. 목록 필터에 없더라도 상품 상세의 규격표에는 제공할 수 있다.

### 5.5 Filter Interaction

- 필터 버튼은 적용된 조건 수를 숫자로 표시한다.
- 데스크톱 패널과 모바일 시트는 적용 버튼을 누를 때 URL과 결과를 갱신하는 방식을 우선한다.
- 패널 안에서 변경 중인 임시 조건과 적용된 URL 조건을 분리한다.
- 적용 조건은 결과 행 아래 가로 칩으로 노출한다.
- 개별 칩 해제는 즉시 결과에 반영한다.
- `초기화`는 2depth 카테고리와 정렬을 유지하고 속성 필터만 제거한다.
- 같은 그룹은 다중 선택 OR, 다른 그룹은 AND로 결합한다.
- 선택지별 결과 개수는 초기 샘플 데이터가 충분할 때만 노출한다.
- 데스크톱 필터는 화면 오른쪽에서 열리는 약 420px 고정 패널로 제공한다.
- 패널이 열린 동안 목록은 약한 오버레이 아래에 유지하고 배경 조작과 스크롤을 막는다.
- 필터 그룹만 스크롤하며 하단의 `초기화 / N개 상품 보기` 영역은 고정한다.
- 767px 이하는 전체 폭, 768~1023px은 최대 640px 중앙 바텀시트로 제공한다.

## 6. Sort

초기 정렬은 다음으로 제한한다.

- 추천순: 운영 순서 필드가 준비되기 전에는 기본 등록 순서
- 신상품순: 등록일 내림차순
- 낮은 가격순
- 높은 가격순

가격 문의 상품은 가격 정렬의 뒤에 배치한다. 인기순, 판매순과 리뷰순은 근거 데이터가 생기기 전 제공하지 않는다.

## 7. Product Card

```text
┌────────────────────────┐
│ [상태 배지]            │
│                        │
│      대표 이미지       │
│                        │
├────────────────────────┤
│ [옵션][옵션][옵션][옵션][+2]│
│ BRAND · COLLECTION     │
│ 상품명                 │
│ 74,000원 ~ 92,000원    │
└────────────────────────┘
```

- 정보 순서는 브랜드·컬렉션 → 상품명 → 가격이다.
- 모든 상품은 옵션을 최소 하나 이상 가지며 이미지 바로 아래 한 행을 다섯 슬롯으로 사용한다.
- 옵션이 다섯 개 이하면 모두 표시하고, 여섯 개 이상이면 대표 썸네일 네 개와 `+N`을 표시한다.
- 상품 커버는 선택값이며 지정되지 않으면 `defaultOptionId`가 가리키는 옵션 대표 미디어를 목록 기본 이미지로 사용한다.
- 각 옵션은 미디어를 최소 하나 이상 가지며 그중 하나를 대표 미디어로 지정한다.
- 옵션 선택은 카드 대표 이미지만 변경하고 가격은 상품 범위를 유지한다.
- 옵션이 없는 상품 데이터는 허용하지 않으며 목록 카드의 옵션 행은 항상 제공한다.
- 카드 전체가 상세 링크이며 옵션 버튼은 링크 동작과 분리한다.
- 배송, VAT, BOX, 규격과 찜 버튼은 카드에 노출하지 않는다.
- 가격 문의는 버튼이나 강조색 없이 가격 위치에 표시한다.
- 배지는 `NEW` 또는 `MD PICK`만 데이터 근거가 있을 때 카드당 하나만 노출한다.
- 정사각 이미지가 카드의 가장 강한 요소가 되며 카드 가로 간격은 18~22px, 세로 간격은 40~44px을 기준으로 한다.
- 브랜드·컬렉션은 12px 보조색, 상품명은 데스크톱 15px·모바일 14px/600, 가격은 14px/700을 기준으로 한다.
- 상품명은 최대 한 줄 말줄임을 적용하고 전체 이름은 접근 가능한 제목 정보로 제공한다.
- 상품명에 강제 높이를 두지 않으며 모든 카드에 필수 옵션 행을 동일하게 제공한다.

## 8. Product Detail Structure

### 8.1 Top Purchase Area

상단은 `갤러리 → 상품 정보·가격 → 색상 → 규격 → 포장 정보 → 계산기·배송·수량 → 총 상품 금액 → 구매 행동` 순서로 설계한다. 데스크톱은 좌측 갤러리와 우측 구매 정보의 2열, 모바일은 한 열과 하단 고정 구매 행동을 사용한다.

- 상품 커버는 목록·프로모션용이며 상세 갤러리는 선택한 색상의 미디어만 사용한다.
- 색상은 대표 이미지가 있는 선택지이며 변경 시 갤러리 전체가 바뀐다.
- 규격은 색상 아래의 텍스트 선택지이며 실제 판매 Variant를 확정한다.
- 목록에서 전달한 `option` Search Param을 유효한 색상 초기값으로 사용한다.
- 색상과 규격이 모두 선택되어야 장바구니·구매 행동을 활성화한다.

### 8.2 Unit Price and Total

상단 구매 영역은 실제 주문 단위 가격 하나만 제공한다. 장·㎡·BOX 환산값과 포장 정보는 구매 판단을 방해하지 않도록 주문 수량 계산기로 이동한다.

```text
1BOX    58,000원
        공급가 52,727원
```

- 실제 주문 단위와 가격은 하나의 왼쪽 정렬 그룹으로 붙여 표기하며, `1BOX`, `1장`처럼 `판매가`와 `/BOX` 표현을 반복하지 않는다.
- 공급가는 가격 바로 아래 12px 보조 정보로 제공하며 별도의 VAT 비교 아이콘은 두지 않는다.
- 주문 수량 제목은 `주문 수량 (주문 단위: BOX/장)`으로 제공한다.
- 선택 수량 이후에는 `총 상품 금액`만 VAT 포함 금액으로 제공하고 공급가액을 반복하지 않는다.
- 장바구니·견적서·주문서에서 공급가액, VAT와 합계를 다시 분리한다.
- 가격 문의 상품은 단위가격과 구매 행동을 `가격 문의하기`로 교체한다.

### 8.3 Calculator, Shipping and Quantity

- `주문 수량 계산기`는 `ceil(필요 면적 ÷ 주문 단위당 면적)`으로 주문 수량을 제안한다.
- 계산기는 주문 단위당 장수·면적·무게와 결과의 총 장수·시공 가능 면적·예상 상품 금액을 제공한다.
- 로스율은 자동 가산하지 않고 현장 여유분을 별도로 고려하라는 안내만 제공한다.
- 계산 결과는 `계산 수량 적용`을 눌렀을 때 구매 수량에 반영하며 이후 직접 수정할 수 있다.
- 배송 방식은 상품 단위 허용 목록에서 하나를 선택하고 옵션별 예외는 두지 않는다.
- 초기 배송 방식은 선택하지 않으며 `화물 택배 배송 / 개별 화물 운송 / 직접 수령` 중 상품에서 허용한 하나를 명시적으로 선택한다. 상세 배송 안내에도 같은 용어를 사용한다.
- 배송 방법을 고르기 전에는 장바구니·바로 구매를 비활성화하고, 선택 후에만 다음 구매 단계로 진행한다.
- 추가 상품과 배송 방법은 동일한 공통 선택 메뉴를 사용한다. 데스크톱은 트리거 아래 팝오버, 모바일 옵션 시트 안에서는 중첩 모달 없이 인라인 팝오버로 열고, 방향키·Enter·Escape·외부 클릭과 포커스 복귀를 지원한다.
- 추가 상품은 본품 옵션과 분리된 드롭다운에서 선택하고, 선택 후 수량 조절과 명시적 삭제를 제공한다. 같은 상품을 다시 고르면 선택 행을 중복 생성하지 않고 수량을 1 증가시킨다.
- 추가 상품을 선택하면 상단 구매 영역, 모바일 옵션 시트, 스크롤 후 빠른 구매 영역에서 품목·수량·합산 금액을 동일하게 확인하고 수정할 수 있다.
- `장바구니 담기`와 `바로 구매`는 본품과 현재 선택된 추가 상품 전체 구성을 대상으로 하며, 빠른 구매 영역에는 `본품 N단위 · 추가 상품 N종` 요약을 함께 제공한다.
- 수량 변경 시 총 장수·면적과 VAT 포함 총 상품 금액을 갱신한다.

### 8.4 Detail Content and Admin Responsibility

상세 탭 순서는 `상품정보 → 배송·교환 → 리뷰 N → Q&A N`으로 고정한다.

- 상품정보: 관련 GRAF, 제한형 웹 에디터 본문, 구조화된 규격·포장표, 법정 상품정보
- 배송·교환: 재사용 정책 템플릿과 상품별 추가 안내
- 리뷰: 평점 요약, 포토 리뷰, 사진 필터, 구매 당시 옵션과 리뷰 목록. 동영상 리뷰는 지원하지 않는다.
- Q&A: 모든 문의를 비밀글로 처리한다. 작성자와 관리자만 본문·답변을 열람하고 다른 사용자는 잠금된 `비밀글입니다.` 행만 확인한다. 답변 작성자 명칭은 반복하지 않고 답변일만 표시한다.
- 가격 문의는 일반 Q&A와 분리된 문의 흐름으로 제공한다.

웹 에디터는 제목·본문·이미지·영상·표·안내 블록만 허용한다. 가격, Variant, 포장, 재고, 배송 정책, 법정 상품정보와 관련 GRAF ID는 필터·계산·주문에 사용되므로 구조화된 필드로 관리한다. 임의 스크립트·스타일·HTML 삽입은 허용하지 않는다.

## 9. Loading, Empty and Error States

- 첫 목록 진입은 카드 이미지 비율과 텍스트 행을 예약한 스켈레톤을 사용한다.
- 필터 적용 중에는 기존 결과를 유지하고 결과 영역에 낮은 강도의 진행 상태를 제공한다.
- 빈 결과는 적용 조건을 보여주고 `초기화`를 제공한다.
- 이미지 로드 실패 시 상품명을 포함한 대체 영역을 제공한다.
- URL에 유효하지 않은 필터가 있으면 무시하고 유효한 조건만 복원한다.

## 10. Accessibility and Motion

- 필터 패널·시트는 포커스 이동·순환·복귀, Escape 닫기와 배경 비활성화를 지원한다.
- 필터 결과 개수는 `aria-live="polite"`로 알리되 연속 조작 중 중복 알림을 줄인다.
- 옵션 썸네일은 최소 44px 터치 영역과 선택 이름, `aria-pressed`를 제공한다.
- 색상 선택에는 색상칩과 텍스트 이름을 함께 제공한다.
- 패널과 시트 전환은 180~240ms, 옵션 이미지 전환은 150~200ms로 제한한다.
- 상품 카드 재배치에는 장식적 애니메이션을 적용하지 않는다.
- `prefers-reduced-motion`에서 비필수 전환을 제거한다.

## 11. Components

- `CategoryTabs`
- `CatalogToolbar`
- `FilterTrigger`
- `AppliedFilterChips`
- `CatalogFilterPanel`
- `CatalogFilterSheet`
- `FilterGroup`
- `SortMenu`
- `ProductGrid`
- `ProductCard`
- `ProductOptionThumbnails`
- `PriceDisplay`
- `ProductGallery`
- `ProductOptionSelector`
- `ProductSpecificationSummary`
- `AreaCalculator`
- `ShippingMethodSelector`
- `PurchaseActions`
- `RelatedGrafs`

## 12. State Responsibility

| State | Responsibility |
|---|---|
| 1·2depth, filter, sort | URL Search Params |
| 패널 안 미적용 조건 | React local state |
| 카드 옵션 이미지 | ProductCard local state, 상세 진입 시 `option` Search Param |
| 상세 선택 옵션·갤러리·계산기 | `option` Search Param 초기값 + Product detail local state |
| 비회원 장바구니 | Zustand persist, 후속 단계 |
| 서버 상품·재고 | 미도입, 백엔드 설계 후 결정 |

## 13. Validation Scenarios

- 타일과 빅슬랩을 전환하면 해당 필터 정의와 결과가 복원된다.
- 여러 필터 그룹을 적용하고 URL을 공유하면 동일한 상품과 적용 칩이 표시된다.
- 옵션 기반 필터 결과의 카드 이미지와 상세 초기 옵션이 일치한다.
- 목록에서 옵션을 선택한 뒤 상세에 진입하면 동일한 옵션과 옵션 미디어 갤러리가 처음부터 선택된다.
- 가격 문의 상품은 가격 정렬과 구매 행동에서 일반 상품과 구분된다.
- 모바일에서 필터 적용·해제·초기화 후 원래 스크롤 위치로 복귀한다.
- 320, 375, 414, 479, 480, 539, 540, 620, 621, 767, 768, 1023, 1024, 1239, 1240과 1440px에서 가로 넘침과 비정상 열 전환이 없다.

## 14. Open Decisions

- 리뷰와 Q&A의 페이지네이션 또는 더 보기 방식
- 장바구니 이후 견적서 생성 시점과 파일 형식

## 15. References

- [상품 탐색 UI Plan](../../01-plan/features/product-catalog-ui.plan.md)
- [용타일 600×600 상품 목록](https://www.yongtile.com/goods/goods_list.php?cateCd=002010)
- [THE 타일](https://thetile.co.kr/)
- [넥스트타일 빅슬랩 예시](https://nexttile.kr/product/te-mxc1/)
- [유송타일 제품 자료](https://www.usong.co.kr/data/files/2025-09/9d2c3b0a5ce9142f2aeb4373e9194e2b.pdf)

## 16. Approval

- 상품 목록의 카테고리 레일, 필터 항목, 반응형 그리드, 옵션 카드와 패널·시트 구조는 구현 및 사용자 검토를 완료했다.
- 상품 상세의 색상·규격, 단위가격, 계산·배송·수량, 콘텐츠 탭과 관리자 책임 범위를 사용자 승인했다.
